[project]
name = "kakveda"
version = "0.1.0"
description = "Kakveda – LLM failure intelligence platform (repo CLI utilities)"
requires-python = ">=3.11"

[project.scripts]
kakveda = "kakveda_cli.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
